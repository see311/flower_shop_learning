import 'package:flutter/material.dart';

Widget myTitle(text) {
  return Container(
    margin: EdgeInsets.symmetric(vertical: 10),
    child: Text(
      text,
      textScaleFactor: 1.2,
    ),
  );
}
